# Prometheus Configuration
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'agentic-travel-rag'
    static_configs:
      - targets: ['host.docker.internal:8000']
        labels:
          service: 'art-api'
