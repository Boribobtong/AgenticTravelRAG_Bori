[pytest]
markers =
    integration: mark test as integration test that requires external services (docker/network)

# Default: do not run integration tests unless explicitly requested
addopts = -q -m "not integration"
